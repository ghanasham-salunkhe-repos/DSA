package strings;
//::TODO : continue to regex
public class RegexMetaCharacters {
    // we already studied basic regex now we will learn about meta characters
    public static void main(String[] args) {
        /*
        Regular Expression : Description
            \d : Any digits, short-code for [0-9] --> true if digit
            \D : Any non-digits, short-code for [^0-9] --> true if not digit
            \s : Any white space character, short-code for [\t\n\x0B\f\r] --> true for whitespace
            \S : Any non-whitespace character --? true for non whitespace character
            \w : Any word character, short-code for [a-zA-Z_0-9] --> true for any alphanumeric
            \W : Any non-word character --> true for non alphanumeric
            \b : Represents a word boundary
            \B : Represents a non-word boundary

            --> small case and upper case are negation of each other
         */
    }
}
